import { PantallaPronto } from '@/components/juego/PantallaPronto';

/** Stub — G5 lo reemplaza por Ajustes (export JSON, borrado, notificaciones). */
export default function Ajustes() {
  return (
    <PantallaPronto
      titulo="Ajustes"
      detalle="Acá van a vivir el export de tus datos, las paletas del cielo y el borrado total. Todo tuyo, todo local."
    />
  );
}
