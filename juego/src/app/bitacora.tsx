import { PantallaPronto } from '@/components/juego/PantallaPronto';
import { ESTADOS_VACIOS } from '@/content';

/** Stub — G5 lo reemplaza por la Bitácora privada. */
export default function Bitacora() {
  return <PantallaPronto titulo="Bitácora" detalle={ESTADOS_VACIOS.bitacora} />;
}
