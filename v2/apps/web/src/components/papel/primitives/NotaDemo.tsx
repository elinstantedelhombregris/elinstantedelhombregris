/**
 * Nota de datos demo §5 — obligatoria junto a toda métrica inventada.
 * Texto fijo, sin props: para leyendas custom más largas (p. ej. las cifras
 * de Home), la sección compone su propio texto en el mismo estilo.
 */
export function NotaDemo() {
  return (
    <span className="font-space text-tinta-30 text-[10px] uppercase tracking-[0.12em]">
      * datos de demostración
    </span>
  );
}
